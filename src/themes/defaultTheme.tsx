import { createTheme } from '@mui/material/styles';

export const defaultTheme = createTheme({
    palette: {
        secondary: {
            main: '#1e293b',
        },
        primary: {
            main: '#0d9488',
        },
    },
});
